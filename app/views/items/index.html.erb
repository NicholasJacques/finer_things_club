<h1>All Items</h1>

<ul>
  <% @items.each do |item|%>
    <ul>
      <li><%= item.title %></li>
      <li><%= item.price %></li>
      <li><%= button_to "Add to Cart", cart_items_path(item_id: item.id) %></li>
    </ul>
  <% end %>
</ul>